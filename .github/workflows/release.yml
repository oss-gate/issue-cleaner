on: create
name: Release
jobs:
  release:
    name: Release GitHub Actions
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/release-github-actions@v6
        with:
          CLEAN_TARGETS: .[!.]*,lib,test,template,*.js,*.ts,*.json,*.lock,*.yml,*.yaml
